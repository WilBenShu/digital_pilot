{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'form-control' } }) }}

    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'Valider'|trans }}" />
    </div>
{{ form_end(form) }}
